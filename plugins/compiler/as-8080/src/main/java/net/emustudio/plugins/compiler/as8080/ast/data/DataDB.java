package net.emustudio.plugins.compiler.as8080.ast.data;

import net.emustudio.plugins.compiler.as8080.ast.expr.Expr;
import net.emustudio.plugins.compiler.as8080.ast.instr.Instr;

public class DataDB extends Data {

    public DataDB(Expr expr) {
    }

    public DataDB(String string) {
    }

    public DataDB(Instr instr) {
    }
}
