<!--suppress MavenModelInspection -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.sf.emustudio</groupId>
    <artifactId>emustudio-parent</artifactId>
    <version>0.39-SNAPSHOT</version>
    <relativePath>../.</relativePath>
  </parent>

  <artifactId>emuStudio-doc</artifactId>
  <packaging>pom</packaging>

  <name>emuStudio Documentation</name>

  <profiles>
    <profile>
      <id>doc</id>
      <build>
        <plugins>
          <plugin>
            <groupId>de.saumya.mojo</groupId>
            <artifactId>gem-maven-plugin</artifactId>
            <configuration>
              <!-- align JRuby version with AsciidoctorJ to avoid redundant downloading -->
              <jrubyVersion>${jruby.version}</jrubyVersion>
              <gemHome>${project.build.directory}/gems</gemHome>
              <gemPath>${project.build.directory}/gems</gemPath>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>initialize</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.asciidoctor</groupId>
            <artifactId>asciidoctor-maven-plugin</artifactId>
            <configuration>
              <!-- The gem-maven-plugin appends the scope (e.g., provided) to the gemPath defined in the plugin configuration -->
              <gemPath>${project.build.directory}/gems-provided</gemPath>
              <requires>
                <require>asciidoctor-diagram</require>
              </requires>

              <backend>html</backend>
              <doctype>article</doctype>
              <sourceHighlighter>coderay</sourceHighlighter>
              <attributes>
                <imagesdir>.</imagesdir>
                <!-- Display anchors on html document-->
                <sectanchors>true</sectanchors>
                <toc>left</toc>
                <icons>font</icons>
                <docinfo1>true</docinfo1>
                <toclevels>3</toclevels>
                <tabsize>4</tabsize>
                <revnumber>${project.version}</revnumber>
                <!-- Parse link attributes such as window="_blank"   -->
                <linkattrs>true</linkattrs>
              </attributes>

            </configuration>

            <executions>
              <execution>
                <id>altair-8800</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/user-manual/altair8800</sourceDirectory>
                  <outputDirectory>${project.build.directory}/user-manual/altair8800</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>brainduck</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/user-manual/brainduck</sourceDirectory>
                  <outputDirectory>${project.build.directory}/user-manual/brainduck</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>rasp</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/user-manual/rasp</sourceDirectory>
                  <outputDirectory>${project.build.directory}/user-manual/rasp</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>main-module</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/user-manual/main_module</sourceDirectory>
                  <outputDirectory>${project.build.directory}/user-manual/main_module</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>tutorials</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/tutorials</sourceDirectory>
                  <outputDirectory>${project.build.directory}/tutorials</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>roadmap</id>
                <phase>generate-resources</phase>
                <goals>
                  <goal>process-asciidoc</goal>
                </goals>
                <configuration>
                  <sourceDirectory>src/roadmap</sourceDirectory>
                  <outputDirectory>${project.build.directory}/roadmap</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
      <dependencies>
        <dependency>
          <groupId>rubygems</groupId>
          <artifactId>asciidoctor-diagram</artifactId>
          <type>gem</type>
          <scope>provided</scope>
          <exclusions>
            <exclusion>
              <groupId>rubygems</groupId>
              <artifactId>asciidoctor</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
</project>
